<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.example.appcomida.ui.home.HomeFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_home">
        <!-- Argumentos para o HomeFragment -->
        <argument
            android:name="email"
            app:argType="string" />
        <argument
            android:name="senha"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/nav_list"
        android:name="com.example.appcomida.ui.lista.ListaFragment"
        android:label="Lista"
        tools:layout="@layout/fragment_lista" />

    <fragment
        android:id="@+id/nav_calendario"
        android:name="com.example.appcomida.ui.Calendario.CalendarioFragment"
        android:label="Calendário"
        tools:layout="@layout/fragment_calendario" />

    <fragment
        android:id="@+id/ajudaFragment2"
        android:name="com.example.appcomida.AjudaFragment"
        android:label="Ajude-nos"
        tools:layout="@layout/fragment_ajuda" />

    <fragment
        android:id="@+id/ReceitasFragment"
        android:name="com.example.appcomida.ReceitasFragment"
        android:label="fragment_receitas"
        tools:layout="@layout/fragment_receitas" />
</navigation>
